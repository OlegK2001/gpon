# Исправления ошибок компиляции

## ✅ Исправлено:

### 1. Ошибка `border-border` класса

**Проблема:** Tailwind не находил класс `border-border` при компиляции CSS.

**Решение:**
1. Добавлен класс `.border-border` в `app/globals.css` вне `@layer` директив:
   ```css
   .border-border {
     border-color: hsl(var(--border)) !important;
   }
   ```

2. Добавлен класс в `safelist` в `tailwind.config.js`:
   ```js
   safelist: [
     'border-border',
     // ... другие классы
   ]
   ```

3. Добавлен `ringOffsetColor` для поддержки `ring-offset-background`:
   ```js
   ringOffsetColor: {
     background: 'hsl(var(--background))',
   }
   ```

### 2. Заменены `@apply` директивы для CSS переменных

**Проблема:** Использование `@apply bg-background` вызывало циклические зависимости.

**Решение:**
- Заменено `@apply bg-background text-foreground` на прямые CSS свойства:
  ```css
  background-color: hsl(var(--background));
  color: hsl(var(--foreground));
  ```

## 📝 Что было сделано:

1. ✅ Класс `.border-border` определен в `app/globals.css` перед `@layer` директивами
2. ✅ Добавлен `safelist` в `tailwind.config.js` для гарантированной генерации классов
3. ✅ Добавлен `ringOffsetColor` для поддержки `ring-offset-background`
4. ✅ Исправлены `@apply` директивы в `app/globals.css`

## 🔧 Файлы изменены:

- `app/globals.css` - добавлен класс `.border-border` и исправлены `@apply`
- `tailwind.config.js` - добавлен `safelist` и `ringOffsetColor`

## 🚀 Следующие шаги:

1. Перезапустите dev сервер (остановите и запустите заново `npm run dev`)
2. Если ошибка останется, очистите кеш Next.js:
   ```bash
   rm -rf .next
   npm run dev
   ```

Класс `border-border` теперь должен работать корректно во всех компонентах.

